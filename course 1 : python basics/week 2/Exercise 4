# Exercise 4
# author: @raj-ch017

"""
Declare the variable a with the value 3.0. Declare the variable b with the value "2". 
Print a plus b, but have the output be 5.

You are asked to add a float to a string and print an integer. You need to be able to typecast a and b so that the result is 5.
"""

a = 3.0
b = "2"
print(int(a)+int(b))
